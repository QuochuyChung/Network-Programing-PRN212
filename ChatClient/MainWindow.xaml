<Window x:Class="ChatClient.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:ChatClient"
        Title="Chat" Height="500" Width="800">
    <Window.Resources>
        <local:FirstLetterConverter x:Key="FirstLetterConverter"/>
        <Style x:Key="UserButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Padding" Value="4,6"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd" Background="{TemplateBinding Background}"
                                CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#D0D0D0"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#B0B0B0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="HiddenScrollBar" TargetType="ScrollViewer">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollViewer">
                        <Grid>
                            <ScrollContentPresenter/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Sidebar -->
        <Border Grid.Column="0" Background="#F0F0F0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Profile Card -->
                <Border Grid.Row="0" Background="#E0E0E0" Margin="10,10,10,0"
                        Padding="10" CornerRadius="8">
                    <StackPanel HorizontalAlignment="Center">
                        <Grid Width="40" Height="40" HorizontalAlignment="Center">
                            <Ellipse Fill="#4A90D9"/>
                            <TextBlock x:Name="MyAvatarText" Text="?"
                                       HorizontalAlignment="Center" VerticalAlignment="Center"
                                       FontSize="18" FontWeight="Bold" Foreground="White"/>
                        </Grid>
                        <TextBlock x:Name="MyUsernameText" Text=""
                                   FontSize="14" FontWeight="SemiBold"
                                   HorizontalAlignment="Center" Margin="0,6,0,0"/>
                    </StackPanel>
                </Border>

                <!-- Separator -->
                <Separator Grid.Row="1" Margin="10,8"/>

                <!-- Online Users Section -->
                <DockPanel Grid.Row="2">
                    <TextBlock DockPanel.Dock="Top" Text="Online Users"
                               FontSize="14" FontWeight="Bold" Margin="10,0,10,6"/>
                    <ScrollViewer DockPanel.Dock="Top"
                                  HorizontalScrollBarVisibility="Auto"
                                  VerticalScrollBarVisibility="Disabled"
                                  Style="{StaticResource HiddenScrollBar}">
                        <ItemsControl x:Name="OnlineUsersList">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel Orientation="Horizontal"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Button Style="{StaticResource UserButtonStyle}" Click="UserButton_Click">
                                        <StackPanel Width="50">
                                            <Grid Width="36" Height="36" HorizontalAlignment="Center">
                                                <Ellipse Fill="#6BBF8A"/>
                                                <TextBlock Text="{Binding Converter={StaticResource FirstLetterConverter}}"
                                                           HorizontalAlignment="Center" VerticalAlignment="Center"
                                                           FontSize="14" FontWeight="Bold" Foreground="White"/>
                                            </Grid>
                                            <TextBlock Text="{Binding}" FontSize="11"
                                                       HorizontalAlignment="Center"
                                                       TextTrimming="CharacterEllipsis"
                                                       Margin="0,4,0,0"/>
                                        </StackPanel>
                                    </Button>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </DockPanel>

                <!-- Separator -->
                <Separator Grid.Row="3" Margin="10,4"/>

                <!-- Conversations Placeholder -->
                <Border Grid.Row="4" Background="#F0F0F0"/>
            </Grid>
        </Border>

        <!-- Chat Area -->
        <Border Grid.Column="1" Background="White">
            <Grid>
                <TextBlock Text="Select a group to start chatting"
                           HorizontalAlignment="Center" VerticalAlignment="Center"
                           FontSize="16" Foreground="Gray"/>
            </Grid>
        </Border>
    </Grid>
</Window>
