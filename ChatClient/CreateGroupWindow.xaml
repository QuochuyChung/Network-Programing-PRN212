<Window x:Class="ChatClient.CreateGroupWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Tạo nhóm chat" Height="270" Width="440" ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner" Background="#F7F9FC" ShowInTaskbar="False">
    <Grid Margin="28">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Tạo nhóm chat mới" FontSize="22" FontWeight="SemiBold" Foreground="#172033"/>
        <TextBlock Grid.Row="1" Text="Bạn sẽ tự động là thành viên đầu tiên của nhóm." Foreground="#6B7280" Margin="0,6,0,22"/>
        <StackPanel Grid.Row="2">
            <TextBlock Text="TÊN NHÓM" FontSize="11" FontWeight="SemiBold" Foreground="#536079" Margin="0,0,0,7"/>
            <TextBox x:Name="GroupNameTextBox" Height="44" Padding="12,10" MaxLength="100" KeyDown="GroupNameTextBox_KeyDown"/>
            <TextBlock x:Name="ErrorTextBlock" Foreground="#C83434" Margin="0,7,0,0"/>
        </StackPanel>
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Hủy" Width="86" Height="40" Margin="0,0,10,0" Click="CancelButton_Click"/>
            <Button Content="Tạo nhóm" Width="105" Height="40" Background="#2557D6" Foreground="White" BorderThickness="0" Click="CreateButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
